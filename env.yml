name: bio_tools
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pandas
  - plink
  - admixture
  - faststructure
  - matplotlib=3.8.4  # Recommended for your .plot.bar() calls